{
  "name": "markdown-previewer",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://bevets87.github.io/Markdown-Previewer",
  "dependencies": {
    "bootstrap": "^3.3.7",
    "marked": "^0.3.6",
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "react-scripts": "1.0.7"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy" : "npm run build&&gh-pages -d build"
  }
}
